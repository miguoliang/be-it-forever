describe('Math Utils', () => {
  it('should pass a basic truth test', () => {
    expect(true).toBe(true);
  });

  it('should calculate 1 + 1 correctly', () => {
    expect(1 + 1).toBe(2);
  });
});
